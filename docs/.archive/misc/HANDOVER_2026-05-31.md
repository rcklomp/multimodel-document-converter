# Session Handover - 2026-05-31

Pick up where this session left off. Two independent threads in flight: (A) V3 Grand Soak in the MM-Converter project, (B) M1/M5 LAN+SSH cleanup on Ronald's home network. Read both summaries; act on whichever the user asks about.

---

## CRITICAL FORMAT RULES (load these before responding)

These were learned painfully in the prior session. Both are stored in memory under feedback/ and indexed in MEMORY.md - they should auto-load, but treat this section as the authoritative reminder:

1. **NO em-dashes or non-ASCII punctuation, anywhere.** Memory: [feedback_no_em_dash](.claude/projects/-Users-ronald-Projects-MM-Converter-V2-4-1/memory/feedback_no_em_dash.md). Reason: rendered as `\E2\80\94` in a RouterOS config and broke it. Use plain ASCII (hyphens, parens, colons).

2. **NO triple-backtick code blocks for shell commands meant to be pasted.** Plain text per line, blank line between commands. Memory: [feedback_no_code_blocks_for_commands](.claude/projects/-Users-ronald-Projects-MM-Converter-V2-4-1/memory/feedback_no_code_blocks_for_commands.md). Reason: paste path mangles them; user explicitly said code blocks are unusable. Inline backticks for single-token references are still fine. Multi-line non-command content (file contents, Python source, etc.) can still use code blocks.

3. **Be a tool, not a teacher.** When the user describes a problem, run the diagnostic with the Bash tool yourself before asking them to type anything. The prior session burned a lot of goodwill telling Ronald to run commands when Claude was running in his own shell on the M1.

4. **Auto Mode is on.** Make reasonable judgment calls without pausing for confirmation. The user reverses if needed.

---

## THREAD A: V3 Grand Soak (MM-Converter project)

### One-line state

V3 vision-native extraction COMPLETED 16 of 43 PDFs (halted by OpenRouter weekly budget), V3 wins V2.16 on every soak metric in head-to-head comparison. Full report at [V3_OVERNIGHT_REPORT.md](V3_OVERNIGHT_REPORT.md).

### Headline result (head-to-head, 11 canonical docs sampled in both V3 and V2.16 soaks)

| Metric | V3 | V2.16 | delta |
|---|---:|---:|---:|
| Recall@1 (chunk) | 77.3% | 54.5% | +22.8 pp |
| Recall@5 (chunk) | 95.5% | 63.6% | +31.9 pp |
| Recall@5 (doc) | 100.0% | 86.4% | +13.6 pp |
| Relevance | 84.1% | 72.7% | +11.4 pp |
| Format (judge TRUSTWORTHY axis) | 97.7% | 95.5% | +2.2 pp |
| Faithfulness | 84.1% | 61.4% | +22.7 pp |

Same omlx embedder, same omlx reranker, same GX10 Qwen2.5-14B-FP8 judge, same seed (7). V3 wins on every axis.

### What was shipped

| File | What | Status |
|---|---|---|
| [src/mmrag_v3/engines/vlm_native.py](src/mmrag_v3/engines/vlm_native.py) | Command 18: per-page prompt now asks the VLM to transcribe chart-shaped images as Markdown tables alongside visual description | 13/13 AST tests green |
| [src/mmrag_v3/engines/vlm_provider.py](src/mmrag_v3/engines/vlm_provider.py) | Added `VLM_NATIVE_MAX_TOKENS` env override (default 4096, this run used 8192). Fixes truncation regression Command 18 surfaced. | 13/13 AST tests green |
| [scripts/synthetic_soak.py](scripts/synthetic_soak.py) | New CLI flags: `--judge-provider {dashscope,vllm}`, `--judge-url`, `--judge-model`, `--docs-root`. GX10 judge path is now first-class. | Working |
| [scripts/v3_batch_ingest.py](scripts/v3_batch_ingest.py) | NEW. HybridEngine batch ingester, resume-safe, `--max-pages` budget cap. | Working |
| [scripts/v3_to_v2_jsonl.py](scripts/v3_to_v2_jsonl.py) | NEW. V3 IngestionChunk -> V2 chunk-shape adapter. | Working |
| [scripts/build_v3_canonical_layout.py](scripts/build_v3_canonical_layout.py) | NEW. Maps V3 output dirs to CANONICAL_DOCS names via Jaccard + substring. | Working, but matcher is brittle (see Known Issues) |
| [scripts/v3_vs_v216_delta.py](scripts/v3_vs_v216_delta.py) | NEW. Per-doc chunk-count + routing delta between V3 and V2.16. | Working |
| [scripts/v3_post_batch_pipeline.sh](scripts/v3_post_batch_pipeline.sh) | NEW. translate -> layout -> index -> V3 soak -> V2.16 baseline soak orchestrator. | Working (modified by linter to add `|| echo ...` after build_v3_canonical_layout for partial-corpus tolerance) |

Qdrant collection `mmrag_v3__qwen3_local`: 3,529 points, 4096-dim, status green.

Output artifacts:
- `output/v3_baselines/` per-doc V3 IngestionChunk JSONL + meta.json
- `output/v3_baselines_v2shape/` same chunks in V2 schema for ingest_to_qdrant
- `output/v3_canonical/` canonical-name layout for the soak's sample stage
- `output/v3_soak/work.v3.jsonl` + `report.v3.md` (V3 soak)
- `output/v3_soak/work.v216.jsonl` + `report.v216.md` (V2.16 baseline soak)
- `output/v3_soak/v3_vs_v216_delta.md` + `.json` (per-doc deltas)
- `output/v3_soak/v216_baseline_stats.json`
- `logs/v3_batch_ingest.log`

### Env vars in use this run

VLM_NATIVE_ENDPOINT=https://openrouter.ai/api/v1

VLM_NATIVE_MODEL=qwen/qwen3-vl-8b-instruct

VLM_NATIVE_API_KEY=$OPENROUTER_API_KEY  (resolved via the OPENROUTER_API_KEY shell var)

VLM_NATIVE_MAX_TOKENS=8192

USE_VLM_ENGINE=1

MLX_API_KEY is set in shell (omlx-server auth).

### What's NOT done (Thread A backlog, in priority order)

1. **OpenRouter weekly budget exhausted at doc 17/24.** Wait for weekly refresh, then run:
   - `USE_VLM_ENGINE=1 VLM_NATIVE_ENDPOINT=https://openrouter.ai/api/v1 VLM_NATIVE_MODEL=qwen/qwen3-vl-8b-instruct VLM_NATIVE_API_KEY="$OPENROUTER_API_KEY" VLM_NATIVE_MAX_TOKENS=8192 ~/miniforge3/envs/mmrag-v2/bin/python scripts/v3_batch_ingest.py --out-dir output/v3_baselines --max-pages 300`
   - Resume-safe: already-completed docs skipped via meta.json check
   - Will process the 8 remaining in-scope docs (Earthship, Drupal Commerce, Sekar MCP, Jungjun AI Agent, ATZ Elektronik, Schwungradspeicher, Handbuch Wickeltechnik, integra_u_en) plus the half-completed Ayeva (delete that dir first to force re-run)
   - Then re-run `scripts/v3_post_batch_pipeline.sh` to re-index and re-soak with the wider corpus

2. **19 long-tail tech-manual books (>300 pages) excluded by `--max-pages 300` cap.** They are Python Distilled 1411p, Fluent Python 766p, Zephyr RTOS 689p, Cronin GenAI 652p, Handbuch Entwicklungspsychologie 587p, Raieli AI Agents 555p, Grundlagen Fahrzeug- und Motorentechnik 511p, Hao ML Platform 504p, Python Cookbook 477p, Nagasubramanian Agentic AI 460p, Greenhouse Design 378p, ArcGIS Python Cookbook 366p, Devlin LLM Agents 365p, Chaubal PyTorch 359p, Bourne RAG 346p, Adedeji GenAI Google Cloud 320p, Schwungradspeicher 309p, Handbuch Wickeltechnik 309p, Ayeva Python Patterns 296p (just over). Each one alone needs hours of OpenRouter VLM budget. Strategy options: (a) increase the budget; (b) split across multiple weekly windows; (c) route the long tail through a self-hosted VLM endpoint instead.

3. **2 EPUBs not covered** (Falkner ChatGPT Praktijk-handboek, Seffer KI En ChatGPT). HybridEngine is fitz-based (PDF only). Needs a separate EPUB extractor.

4. **CarOK voorraadtelling has 1 docling-fallback page** (97 fewer chunks than V3 rebaseline reference of 443). Single transient OpenRouter response. Re-run with `--force` on the V3 batch script to fix (or just delete that doc dir and re-run).

5. **`build_v3_canonical_layout.py` matcher is brittle.** Required one manual delete this run (`Greenhouse_Design` matched `ATZ_Aerodynamik` via shared word "design", min_score=0.30 + substring bonus). Audit any score <0.50 row in the matcher's output after every run. Better fix: require >= 2 distinctive token overlaps OR explicit prefix match.

6. **Phase A Step 5 (`batch_processor.py` UIR-native rewire) still PARTIAL.** This soak ran the V3 chunker directly out of HybridEngine, sidestepping the v2.x heading-reconcile / merge / spatial-refiner pipeline. Production parity still requires closing Step 5. Per `docs/PROJECT_STATUS.md`.

7. **Magazine-layout fallback rates high.** Digitale-Fotografie 16% (23/144), Kimothi 11% (29/258). Probably need page-tiling for visually complex layouts, or per-doc max_tokens tuning above 8192.

8. **Dirty git state.** Working tree has ~50 uncommitted doc deletes/edits PLUS this session's new scripts and report. Probably want to commit before any further work to anchor the baseline.

### Test status

`pytest tests/test_v3_security.py -q` -> 13 passed, 0 failed (AST firewall green).

Full suite was last run at 1382 passed / 17 skipped per PROJECT_STATUS.md. Not re-run this session.

### Known good endpoints (verified live this session)

omlx-server (Mac Mini M4) at `http://10.0.10.246:8000` - embeddings + rerank, auth via `MLX_API_KEY` env var

GX10 vLLM at `http://10.0.10.239:8000` - serves `RedHatAI/Qwen2.5-14B-Instruct-FP8-dynamic`, no auth required from LAN

OpenRouter at `https://openrouter.ai/api/v1` - serves `qwen/qwen3-vl-8b-instruct`, auth via `OPENROUTER_API_KEY` env var, **WEEKLY BUDGET EXHAUSTED**

Qdrant at `http://localhost:6333` - collections: `mmrag_v2_8__qwen3_local` (34338 points), `mmrag_v2_8__bm25_sparse` (28547), `mmrag_v3__qwen3_local` (3529), `mmrag_v2_8__qwen3_dashscope_smoke` (8), `mmrag_v3__smoke` (9 - from this session's smoke test, safe to delete)

---

## THREAD B: M1 / M5 LAN + SSH + DynDNS cleanup

### One-line state

M1 SSH to M5 is one command away from passwordless. MikroTik DynDNS (lease-to-DNS sync) has a stale-entry problem with no clean fix yet (lease-script ruled out by Ronald, no other native MikroTik mechanism exists).

### What's already done

1. Identified `~/.ssh/id_rsa` as a locked legacy key Ronald can't unlock - moved to `~/.ssh/id_rsa.locked.bak`. SSH no longer tries it.
2. Confirmed `~/.ssh/id_ed25519` already exists, has NO passphrase. Pubkey:
   - `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIIiUxBasmdWErrQNtXDzJvsq0IDMRJgvEwqCRARqnZot ronald@MacBook-Pro.local`
3. Fixed `~/.ssh/config` block for M5 to:
   - match all three names (`m5macbook`, `macbook-pro-m5.lan`, `macbook-pro-m5.local`)
   - point at `id_ed25519` (was incorrectly pointing at `id_ed25519_gx10`)
   - removed the quoted `HostName "MacBook-Pro-M5"` that didn't resolve

### What's NOT done (Thread B backlog)

1. **One command left for Ronald to run** (needs his M5 password, can't be automated from M1):

   ssh-copy-id ronald@macbook-pro-m5.local

   After that, `ssh ronald@macbook-pro-m5.local` is fully passwordless. Verify with:

   ssh ronald@macbook-pro-m5.local 'hostname; date'

2. **Bidirectional setup (M5 -> M1) not done.** Same two commands on the M5: generate ed25519 if needed (Ronald has it), then `ssh-copy-id ronald@macbook-pro-m1.local`. Also requires M1's SSH server enabled - currently `Remote Login: Off` on M1 per `systemsetup -getremotelogin`. To enable: `sudo systemsetup -setremotelogin on` on the M1.

3. **MikroTik DynDNS root cause UNRESOLVED.** Stale lease for `macbook-pro-m1` at 10.0.10.233 in the router's DNS (real IP is 10.0.10.232). Symptoms: `nslookup macbook-pro-m1.lan` from any other host returns the dead .233 IP. Constraints from Ronald:
   - NO lease-script (explicitly forbidden)
   - Does NOT want static MAC reservations (that defeats the dyndns purpose)

   The unresolved tension: MikroTik RouterOS as of 7.x has only TWO native mechanisms for dynamic DNS from DHCP leases - lease-script or manual `/ip dns static`. Both are off the table. The architectural alternatives that remain (NOT YET agreed by Ronald, treat as proposals not decisions):

   - (a) Move DNS off MikroTik. Run dnsmasq on the always-on Mac Mini M4 (10.0.10.246). dnsmasq has native DHCP-lease-aware DNS without scripting. Either let dnsmasq do DHCP too (disable MikroTik DHCP), or let it watch MikroTik's lease file.
   - (b) Drop `.lan` entirely. Use `.local` (Bonjour) for all internal resolution. Already proven working on macOS hosts.
   - (c) Switch to a different router OS (OPNsense/pfSense) that has built-in DDNS-from-DHCP.

   For one-off cleanup of the current stale entry (band-aid, not a fix): on MikroTik, `/ip dhcp-server lease remove [find address=10.0.10.233]` then `/ip dns cache flush`. This will recur the next time a host's IP changes.

### Environment

Router (MikroTik) at 10.0.10.1, serves DNS for `.lan` domain. DHCP pool is 10.0.10.0/24. M1 currently at 10.0.10.232, M5 at 10.0.10.235, Mac Mini M4 at 10.0.10.246, NAS at 10.0.10.237. GX10 (Asus Ascent / Nvidia DGX Spark clone) at 10.0.10.239 for vLLM.

---

## SESSION ARTIFACTS LOG (newest first)

Files created/modified this session:

- `HANDOVER_2026-05-31.md` (this file)
- `V3_OVERNIGHT_REPORT.md`
- `~/.claude/projects/.../memory/feedback_no_code_blocks_for_commands.md`
- `~/.claude/projects/.../memory/MEMORY.md` (index update)
- `~/.ssh/config` (fixed M5 block)
- `~/.ssh/id_rsa` -> `~/.ssh/id_rsa.locked.bak` (moved)
- `scripts/v3_batch_ingest.py` (NEW)
- `scripts/v3_to_v2_jsonl.py` (NEW)
- `scripts/build_v3_canonical_layout.py` (NEW)
- `scripts/v3_vs_v216_delta.py` (NEW)
- `scripts/v3_post_batch_pipeline.sh` (NEW, then linter-modified for `set -e` tolerance)
- `scripts/synthetic_soak.py` (patched: judge-provider, docs-root)
- `src/mmrag_v3/engines/vlm_provider.py` (patched: VLM_NATIVE_MAX_TOKENS)
- `src/mmrag_v3/engines/vlm_native.py` (patched: Command 18 chart-to-markdown rule)
- `output/v3_baselines/**` (16 docs, ~4,054 chunks)
- `output/v3_baselines_v2shape/**`
- `output/v3_canonical/**` (13 canonical mapped)
- `output/v3_soak/work.v3.jsonl`, `report.v3.md`
- `output/v3_soak/work.v216.jsonl`, `report.v216.md`
- `output/v3_soak/v3_vs_v216_delta.md`, `.json`
- `output/v3_soak/v216_baseline_stats.json`
- Qdrant collections: `mmrag_v3__qwen3_local` (3529 pts), `mmrag_v3__smoke` (9 pts, safe to delete)

---

## SUGGESTED OPENING MOVES FOR THE NEXT SESSION

If Ronald asks about the V3 work: read `V3_OVERNIGHT_REPORT.md` first, then offer one of: (1) finish the SSH copy-id on M5, (2) deal with the MikroTik DNS architecture decision, (3) commit the dirty git state, (4) wait for OpenRouter budget refresh and resume the batch.

If Ronald asks about SSH/network: pick up at "one command left" (ssh-copy-id) and "MikroTik architecture decision pending".

If Ronald asks something new: forget both threads, address the new ask. The threads are written down here; they don't need to be in context for unrelated work.

Either way: do NOT lecture, do NOT ask the user to run things you can run yourself, do NOT use em-dashes or fenced code blocks for shell commands.
