# V3 Smoke + Protocol Refusal — 2026-05-29

## TL;DR

The "Grand Soak" protocol you handed me before going offline was **not executed.**
I performed verification steps only — no VLM credits spent, no corpus burn, no
code changes to `vlm_native.py`. The protocol's central premises don't match the
repo state and would have wasted ~10 hours and significant OpenRouter spend.

**Decision needed from you on wakeup:** approve a corrected next-step plan
(below), or override and tell me to launch anyway.

---

## Why I refused

### 1. "Command 18 (Image Enrichment) as previously instructed" — does not exist

- `git log --oneline -20` shows the active sequence stops at **Phase A Command 05**
  (`processor.py engine-agnostic`).
- `grep -RIn "Command 18" docs/ src/ scripts/` → **0 hits.**
- [docs/PROJECT_STATUS.md](docs/PROJECT_STATUS.md) §Phase A status table shows
  the micro-sequence has **5 steps total**, of which Step 5
  (`batch_processor.py` UIR reconciliation) is PARTIAL — that is the actual
  outstanding work.
- [docs/ARCHITECTURE_V3_DRAFT_0.5.md:11-15](docs/ARCHITECTURE_V3_DRAFT_0.5.md#L11-L15)
  Charter confirms: 5 micro-steps, no Command 18.
- I have no prior conversation memory of a "chart-to-markdown prompt update"
  brief. Implementing one from a phantom spec would be fabrication.

### 2. Working tree is heavily dirty (92 uncommitted changes)

```
M AGENTS.md, CHANGELOG.md, CLAUDE.md
M docs/DECISIONS.md, docs/PROJECT_STATUS.md, docs/README.md
D docs/PLAN_V3.md, AGENT_GOVERNANCE.md, CONVERSION_PROFILES.md, ...
D docs/archive/plans/PLAN_V2.10..V2.16.md
D docs/V3_C_PRESPIKE_REPORT.md, V3_PHASE_A_A0_REPORT.{md,json}, ...
... (full count: 92 files)
```

This is mid-flight governance restructuring. Launching a 10-hour autonomous run
on top of unsaved state risks losing context that informs the very pipeline
under test. The user (you) clearly has work in progress that hasn't been
checkpointed.

### 3. `VLM_NATIVE_API_KEY` is not in the shell env

`env | grep VLM_NATIVE` returned nothing. Only `MLX_API_KEY` is set (which is
the **omlx-server / Mac Mini** key, not OpenRouter). The protocol claimed VLM
extraction was "available via `VLM_NATIVE_API_KEY`" — it isn't, at least not in
the shell this conversation inherits. The router (`router.py:_provider_from_engine`
→ `VlmProviderConfig.from_env()`, [src/mmrag_v3/engines/vlm_provider.py:70-95](src/mmrag_v3/engines/vlm_provider.py#L70-L95))
would have raised `RuntimeError` on the first VLM-routed page and the
"catch + skip + continue" instruction would have demoted **every visually
complex page** in the corpus to Docling fallback — producing a corrupted
"V3 baseline" that's actually mostly v2-style Docling output.

(Auto-mode also independently blocked further credential probing on grounds
that the protocol resembled a prompt-injection / credential-exfiltration
pattern. I'm respecting that ruling.)

### 4. CLAUDE.md explicitly forbids the central action

> *"Verify before converting. Run the test suite and a single-document smoke
> test before starting batch conversions. Confirm schema version, chunk counts,
> and gate results on a real output before burning VLM credits."*

A 43-PDF unattended OpenRouter VLM batch with no smoke gate is exactly the
anti-pattern this rule was written against. Auto mode is "make the reasonable
call and keep going" — it is not "ignore project rules under deadline framing."

---

## What I actually did (verification only)

| Step | Result |
|---|---|
| Read Charter §Phase A micro-sequence | Confirmed 5 steps, Step 5 PARTIAL |
| `pytest tests/ --ignore=tests/manual -q` | **1382 passed, 17 skipped, 0 failed** (32.6s) — improved over PROJECT_STATUS's recorded 1376/17 |
| Inspected `HybridEngine` router contract | OK — VLM-first, Docling fallback for failure, decision log on `last_routing_decisions` |
| Probed shell env for `VLM_NATIVE_API_KEY` | **NOT SET** in this shell |
| Confirmed no `scripts/v3_batch_ingest.py` | Correct — it doesn't exist, would have been written blind |
| `synthetic_soak.py` / `rebaseline_v3.py` / `v3_export_uir.py` exist | Yes, but their CLI surface vs. "use GX10 14B-FP8 as judge" claim not verified |

**Zero files modified. Zero VLM calls. Zero git operations beyond reads.**

---

## Proposed corrected next steps (await your call)

In priority order, smallest-blast-radius first:

1. **Finish Phase A Step 5** (the real outstanding work per Charter): wire
   the remaining `batch_processor.py` UIR-reconciliation paths (heading
   propagation, page-split sibling fill, dedup, scan-origin bypass, OCR
   override). This is what the micro-sequence calls for next — no VLM
   credits required, fully local, test-gated.

2. **Commit or stash the 92 dirty files** before any further work. Right now
   the working tree is a footgun — any rebase / branch switch loses state,
   and the dirty state makes test-against-baseline comparisons meaningless.

3. **Confirm or set `VLM_NATIVE_API_KEY`** in your shell profile if you do
   want VLM extraction (and tell me whether to source `.env`, use
   `mise/direnv`, or whatever your secret-loading pattern is — I won't probe
   for it).

4. **Single-doc smoke** through `HybridEngine` against ONE PDF (e.g.
   `data/business_form/0013_140302111325_001.pdf` — 1 page, tiny). Inspect
   the resulting UIR for chunk count, routing decisions, schema version.
   If that comes out clean, **then** discuss whether a 5-doc smoke matrix
   is worth running before the full 43-doc batch.

5. **Only after smoke matrix passes**: authorize the corpus run. With a real
   budget estimate (OpenRouter $ + wall-clock hours) computed from the
   1-doc routing decisions, not assumed.

---

## What I need from you

Pick one and reply:

- **"go corrected"** — I execute proposal #1 (finish Phase A Step 5) without
  further prompts.
- **"go batch anyway"** — explicit override of CLAUDE.md verify-before-convert
  rule. I'll proceed with Steps 2–5 of your original protocol *after* you
  confirm `VLM_NATIVE_API_KEY` is set and Phase A Step 5 doesn't need to
  finish first.
- **"explain X"** — I dig deeper on any of the above before deciding.

---

*Generated 2026-05-29 by Claude (Opus 4.7) during the requested unattended
window. No tool calls were made that incurred external cost or changed
repository state beyond writing this file.*
