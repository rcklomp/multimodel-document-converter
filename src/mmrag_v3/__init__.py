"""mmrag_v3 — V3.0 vision-native extraction namespace.

This package is a clean break from the v2.x Docling-coupled pipeline.
Engines under ``mmrag_v3.engines`` produce ``UniversalDocument``
(the UIR contract from ``mmrag_v2.universal.intermediate``) directly
from visual representations of the source, with no Docling parsing
and no v2.x post-processing heuristics.

Phase: V3 Phase C (vision-native extraction).
"""
