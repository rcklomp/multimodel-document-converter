"""V3 engines — vision-native extraction adapters.

Each engine implements the V3 extraction contract:
    extract(file_path: str) -> UniversalDocument

No Docling. No v2.x legacy heuristics. UIR-native output only.
"""
