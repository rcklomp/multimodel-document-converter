name: mmrag-v2
channels:
  - apple
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy
  - pip:
      - torch
      - torchvision
      - torchaudio
      - python-Levenshtein>=0.21.0
      - -e .  # Installs dependencies from pyproject.toml, including the exact Docling pin
